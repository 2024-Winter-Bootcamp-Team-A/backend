global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['prometheus-monitoring:9090'] # Grafana가 데이터 읽어올 수 있게 localhost x

  - job_name: 'django-data'
    static_configs:
      - targets: ['backend:8000'] # backend 컨테이너와 연결
    